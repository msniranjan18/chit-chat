services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: chitchat
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ..      # This goes up one level to the root and Look for go.mod/pkg in the root
      dockerfile: hack/Dockerfile
    ports:
      - "8080:${PORT:-8080}"
    environment:
      DATABASE_URL: "postgres://postgres:password@postgres:5432/chitchat?sslmode=disable"
      REDIS_URL: "redis://redis:6379"
      JWT_SECRET: "oZapNv/sF9QVKXZJZWhgrFnsvNPBOgz9Z+8NOkEC0GE="
      PORT: ${PORT:-8080}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
